#include<bits/stdc++.h>
#include<stdio.h>

using namespace std;
// struct node {
//     char data;
//     struct node* next;
// };

// void iterate_ll(node *head){

//     node* i1 = head;
//     node* i2 = head;

//     while(i1->data != i2->data){
//         i1 = i1->next;
//         i2 = i2->next->next->next;
//     }

//     printf("%c", i2->data);
// }

// node *createNode(int val){
//     struct node* newNode = NULL;
//     newNode = (struct node*) malloc(sizeof(struct node));
//     newNode->data = val;
//     newNode->next = NULL;
//     return newNode;
// }
int main(){
    // struct node* head = NULL;
    // head = (struct node*) malloc(sizeof(struct node));
    // head->data = 'a';
    // head->next = NULL;

    
    // struct node* head1 = head;

    // for(char c = 'b'; c <= 'z'; c++){
    //     head1->next = createNode(c);
    //     head1 = head1->next;
    // }

    // iterate_ll(head);

    long long num = 1e20;
    cout<<num<<" ";
    return 0;
}